<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools" xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">
        <item
            android:id="@+id/action_search"
            android:icon="@drawable/ic_search"
            app:showAsAction="always|collapseActionView"
            app:actionViewClass="com.jbak.videos.view.CustomSearchView"
            android:title="@string/search"/>
        <item
            android:id="@+id/more"
            android:icon="@drawable/ic_more"
            app:showAsAction="always"
            android:title="@string/search" tools:ignore="AlwaysShowAction">
        <menu>
            <item
                android:id="@+id/refresh"
                android:title="@string/refresh"
                android:icon="@drawable/ic_refresh"
            />
            <item
                android:id="@+id/tech_menu"
                android:title="@string/tech_menu"
                android:icon="@drawable/ic_tech_menu"
            />
            <item
                android:id="@+id/select_provider"
                android:title="@string/select_provider"
                android:icon="@drawable/ic_search_provider"
            />
            <item
                android:id="@+id/search_history"
                android:title="@string/search_history"
                android:icon="@drawable/ic_history"
            />
            <item
                    android:id="@+id/settings"
                    android:title="@string/settings"
                    android:icon="@drawable/ic_settings"
            />

        </menu>
    </item>
</menu>